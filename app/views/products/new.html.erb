<% provide(:title, 'New Products') %>
<% provide(:button_text, t('form.submit.create')) %>

<div class="login-singup-wrapper">
    <div class="login-content">
        <h1><%= t('form.submit.new_product') %></h1>
        <div class="login-form">
            <%= render 'form' %>
        </div>
    </div>
</div>
