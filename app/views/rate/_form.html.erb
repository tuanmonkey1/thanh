<% if logged_in? %>
	<%= form_for([@review, @rate]) do |f| %>
	    <% (1..5).each do |x| %>
	      <i class="fas fa-star" id="s<%= x %>"></i>
	      <%= f.hidden_field :star, id: "s#{x}",value: x %>
	    <% end %>
	  <%= f.submit %>
	<% end%>
<% end%>