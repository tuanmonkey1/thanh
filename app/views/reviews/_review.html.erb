<div class="review-contentBx">
	<div class="image">
		
	</div>
	<a href="#"><h2><%= review.title %></h2></a>
	<span><%= l(review.created_at, locale: :en) %></span>
	<p><%= review.descritpion %></p>
	<% if current_user?(review.user) %>
		<%= link_to "delete", review , method: :delete, data: { confirm: "You sure?" } %>
	<% end %>
</div>
