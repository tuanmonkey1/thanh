<% pre_like = @review.likes.find { |like| like.user_id == current_user.id} %>
<%= button_to t('like.like'), review_like_path(@review, pre_like), method: :delete , class: "btn btn-primary", remote: true %>