<% provide(:title, t('users.new.signup')) %>
<% provide(:button_text, t('users.form.create')) %>

<div class="login-singup-wrapper">
    <div class="login-content">
        <h1><%= t('users.new.signup') %></h1>
        <div class="login-form">
            <%= render 'form' %>
        </div>
    </div>
</div>
